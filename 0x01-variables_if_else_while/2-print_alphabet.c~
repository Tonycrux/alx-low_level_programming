#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints "Number is Positive, Zero or Negative"
 * Return: 0
 */
int main (void)
{
	char ch = 'a';

	for (ch = 'a'; ch < 'z'; ch++)
		putchar(ch);

	return (0);
}
